<Window x:Class="SupermarketManager1.Duy.ReportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.FontWeight="Medium"
        TextElement.FontSize="14"
        FontFamily="{materialDesign:MaterialDesignFont}"
        
        mc:Ignorable="d"
        Title="Revenue Report" Height="751" Width="1200" Loaded="Window_Loaded"
        WindowStartupLocation="CenterScreen">

    <materialDesign:Card Padding="20" Margin="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20">
                
                <TextBlock Text="REVENUE REPORT" 
                           FontSize="24" FontWeight="Bold" 
                           VerticalAlignment="Center"
                           Foreground="{DynamicResource PrimaryHueMidBrush}"/>
            </StackPanel>


            <GroupBox Header="Filter Report" Grid.Row="1" Margin="0,0,0,10"
                      Style="{StaticResource MaterialDesignGroupBox}"
                      materialDesign:ColorZoneAssist.Mode="Standard">
                <Grid Margin="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <ComboBox x:Name="WarehouseFilterComboBox" Grid.Column="0" Margin="0,0,20,0"
                              materialDesign:HintAssist.Hint="Filter by Warehouse"
                              Style="{StaticResource MaterialDesignOutlinedComboBox}"
                              DisplayMemberPath="WarehouseName" SelectedValuePath="WarehouseId"
                              SelectionChanged="FilterComboBox_SelectionChanged">
                        <ComboBoxItem Content="All" Tag="0" IsSelected="True"/>
                    </ComboBox>

                    <DatePicker x:Name="FromDatePicker" Grid.Column="1" Margin="0,0,20,0"
                                materialDesign:HintAssist.Hint="From Date"
                                Style="{StaticResource MaterialDesignOutlinedDatePicker}"
                                SelectedDateChanged="DatePicker_SelectedDateChanged"/>

                    <DatePicker x:Name="ToDatePicker" Grid.Column="2"
                                materialDesign:HintAssist.Hint="To Date"
                                Style="{StaticResource MaterialDesignOutlinedDatePicker}"
                                SelectedDateChanged="DatePicker_SelectedDateChanged"/>
                </Grid>
            </GroupBox>

            <Grid Grid.Row="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <materialDesign:Card Grid.Row="0" Padding="15" Margin="0,5,0,10">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <TextBlock Text="Total Revenue:" FontSize="16" FontWeight="Bold" VerticalAlignment="Center"/>
                        <TextBlock x:Name="TotalRevenueLabel" Text="0 VNĐ" FontSize="18" FontWeight="Bold" 
                                   Foreground="Red" Margin="10,0,30,0" VerticalAlignment="Center"/>

                        <TextBlock Text="Total Orders:" FontSize="16" FontWeight="Bold" VerticalAlignment="Center"/>
                        <TextBlock x:Name="TotalOrdersLabel" Text="0" FontSize="18" FontWeight="Bold" 
                                   Foreground="Blue" Margin="10,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                </materialDesign:Card>

                <DataGrid x:Name="SalesDataGrid" Grid.Row="1" 
                          Style="{StaticResource MaterialDesignDataGrid}"
                          HeadersVisibility="Column"
                          IsReadOnly="True"
                          GridLinesVisibility="None"
                          RowHeaderWidth="0"
                          AutoGenerateColumns="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Sale Date" Binding="{Binding SaleDate, StringFormat={}{0:dd/MM/yyyy HH:mm}}" Width="150"/>
                        <DataGridTextColumn Header="Warehouse" Binding="{Binding Warehouse.WarehouseName}" Width="*"/>
                        <DataGridTextColumn Header="Product Code" Binding="{Binding ProductCode}" Width="100"/>
                        <DataGridTextColumn Header="Product Name" Binding="{Binding ProductCodeNavigation.NameP}" Width="*"/>
                        <DataGridTextColumn Header="Qty" Binding="{Binding QuantitySold}" Width="60"/>
                        <DataGridTextColumn Header="Unit Price" Binding="{Binding UnitPrice, StringFormat={}{0:N0}}" Width="120"/>
                        <DataGridTextColumn Header="Total Amount" Binding="{Binding TotalAmount, StringFormat={}{0:N0} VNĐ}" Width="150">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="Foreground" Value="Green"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="Staff" Binding="{Binding Account.FullName}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>

            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button Content="Close" Width="100" Height="40" 
                        Style="{StaticResource MaterialDesignOutlinedButton}"
                        Click="CloseButton_Click"/>
            </StackPanel>

            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,20,0,0">
                <Button Content="Export" Width="100" Height="40" 
            Style="{StaticResource MaterialDesignOutlinedButton}"
            Click="ExportButton_Click"/>
            </StackPanel>
        </Grid>
    </materialDesign:Card>
</Window>